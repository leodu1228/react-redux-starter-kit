import React from 'react';
import '../../style.css';

const MessageAuthor = ({children}) => {
    return(
        <div className='MessageAuthor'>{children}</div>
    )
}

export default MessageAuthor;